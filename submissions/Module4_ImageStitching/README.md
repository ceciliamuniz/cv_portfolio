# Module 4 — Image Stitching

This module contains an image stitching implementation (SIFT + RANSAC + blending) and a small Flask integration under `submissions/Module4_ImageStitching/app.py`.

How to run (quick):
- From the project root run the main app and visit `/module4` in the browser.

Notes:
- Requires OpenCV (with contrib for SIFT) and NumPy.